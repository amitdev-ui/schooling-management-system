[providers.node]
version = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.npm"]

[phases.install]
cmds = [
  "npm install",
  "cd ../client && npm install"
]

[phases.build]
cmds = [
  "cd ../client && npm run build"
]

[start]
cmd = "npm run start"
